# MCP API base URL
# Convention: {base}/{serverId}/mcp (e.g. /api/general/mcp)
#
# Local dev: leave empty - Vite proxies /api to localhost:3100 (no CORS)
# Production same-origin: leave empty to use /api (web + MCP on same CloudFront)
# Production separate API: set full URL after CDK deploy, e.g.:
#   VITE_MCP_BASE_URL=https://d1234abcd.cloudfront.net/api
#
# Build-time only. Copy to .env and fill:
VITE_MCP_BASE_URL=

# Site URL for sitemap.xml and robots.txt (e.g. https://izan.io)
PUBLIC_SITE_URL=

# Google Analytics 4 / Google Ads (optional)
# - GA4: G-XXXXXXXXXX
# - Google Ads: AW-XXXXXXXXX (use measurement ID from Ads > Tools > Conversions)
# Build-time only. If set, enables gtag with Consent Mode v2 (GDPR).
VITE_GA_MEASUREMENT_ID=

# Microsoft Clarity (optional)
# Project ID from clarity.microsoft.com > Settings > Overview
# Session replays, heatmaps, insights. Respects cookie consent.
VITE_CLARITY_PROJECT_ID=
