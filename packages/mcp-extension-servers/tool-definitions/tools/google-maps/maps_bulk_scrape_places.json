{
  "id": "maps-bulk-scrape-places",
  "name": "maps_bulk_scrape_places",
  "description": "Scrape detailed info for multiple Google Maps places in parallel using concurrent browser tabs. Use this instead of calling maps_scrape_place multiple times - it is faster and more efficient. Pass all place URLs as a single comma-separated string.",
  "version": "1.0.0",
  "parameters": [
    {
      "name": "place_urls",
      "type": "string",
      "description": "Comma-separated Google Maps place URLs",
      "required": true
    }
  ],
  "steps": [
    {
      "action": "code",
      "code": "return `{{place_urls}}`.split(',').map(u => ({ url: u.trim() }))",
      "name": "items",
      "label": "Build place URL list"
    },
    {
      "action": "forEachItem",
      "sourceExtract": "items",
      "openMethod": "url",
      "urlField": "url",
      "concurrency": 3,
      "maxItems": 10,
      "waitUntil": "load",
      "label": "Scrape each place in parallel",
      "detailSteps": [
        {
          "action": "waitForSelector",
          "selector": "h1",
          "timeout": 15000,
          "label": "Wait for place"
        },
        {
          "action": "wait",
          "ms": 2000
        },
        {
          "action": "code",
          "code": "const nameEl = document.querySelector('h1.DUwDvf, h1'); const ratingEl = document.querySelector('div.F7nice span[aria-hidden=\"true\"], span.ceNzKf, div.jANrlb div.fontDisplayLarge'); const reviewCountEl = document.querySelector('div.F7nice span[aria-label*=\"review\"], button[jsaction*=\"review\"] span'); const addressBtn = document.querySelector('button[data-item-id=\"address\"], button[data-item-id^=\"address\"], button[aria-label*=\"Address\"]'); const phoneBtn = document.querySelector('button[data-item-id^=\"phone\"], a[href^=\"tel:\"], button[aria-label*=\"Phone\"]'); const websiteLink = document.querySelector('a[data-item-id=\"authority\"], a[data-item-id^=\"authority\"], a[aria-label*=\"Website\"]'); return { name: nameEl ? nameEl.textContent.trim() : 'Unknown', rating: ratingEl ? ratingEl.textContent.trim() : '', review_count: reviewCountEl ? reviewCountEl.textContent.trim() : '', address: addressBtn ? addressBtn.textContent.trim().replace(/^Address:\\s*/i, '') : '', phone: phoneBtn ? phoneBtn.textContent.trim().replace(/^Phone:\\s*/i, '') : '', website: websiteLink ? websiteLink.href : '', url: window.location.href }",
          "name": "place_details",
          "label": "Extract place details"
        }
      ]
    }
  ]
}
