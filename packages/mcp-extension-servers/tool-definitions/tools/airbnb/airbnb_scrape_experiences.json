{
  "id": "airbnb-scrape-experiences",
  "name": "airbnb_scrape_experiences",
  "description": "Browse and scrape Airbnb experiences in a given location. Returns up to 15 experiences with title, price, rating, review count, category, duration, and URL.",
  "version": "1.0.0",
  "parameters": [
    {
      "name": "location",
      "type": "string",
      "description": "Location to search for experiences (e.g. 'Paris', 'Tokyo', 'New York')",
      "required": true
    }
  ],
  "steps": [
    {
      "action": "navigate",
      "url": "https://www.airbnb.com/s/{{location}}/experiences",
      "label": "Open Airbnb experiences page"
    },
    {
      "action": "waitForSelector",
      "selector": "[itemprop='itemListElement'], [data-testid='card-container'], [data-testid='listing-card-title']",
      "timeout": 15000,
      "label": "Wait for experiences to load"
    },
    {
      "action": "wait",
      "ms": 3000,
      "label": "Let experiences render"
    },
    {
      "action": "scroll",
      "direction": "down",
      "amount": 1200,
      "label": "Scroll to load more experiences"
    },
    {
      "action": "wait",
      "ms": 2000
    },
    {
      "action": "scroll",
      "direction": "down",
      "amount": 1200,
      "label": "Scroll further"
    },
    {
      "action": "wait",
      "ms": 2000
    },
    {
      "action": "scroll",
      "direction": "down",
      "amount": 1200,
      "label": "Scroll for more experiences"
    },
    {
      "action": "wait",
      "ms": 2000
    },
    {
      "action": "code",
      "code": "const experiences = []; const cards = document.querySelectorAll('[itemprop=\"itemListElement\"], [data-testid=\"card-container\"], [aria-labelledby]'); for (let i = 0; i < Math.min(cards.length, 15); i++) { const el = cards[i]; const titleEl = el.querySelector('[data-testid=\"listing-card-title\"], [id^=\"title_\"], h3'); const priceEl = el.querySelector('span._tyxjp1, span[class*=\"price\"], [data-testid=\"price-availability-row\"] span span'); const ratingEl = el.querySelector('[aria-label*=\"rating\"], span[class*=\"rating\"], .t1a9j9y7 span'); const reviewCountEl = el.querySelector('[aria-label*=\"review\"], span[class*=\"review\"]'); const linkEl = el.querySelector('a[href*=\"/experiences/\"]'); const imgEl = el.querySelector('img[src*=\"images\"], img[data-original-uri]'); const categoryEl = el.querySelector('[data-testid=\"listing-card-subtitle\"], span[class*=\"subtitle\"], .f15liw5s span'); const durationEl = el.querySelector('[data-testid=\"listing-card-subtitle\"]:last-of-type, .s1cjsi4j span'); const url = linkEl ? (linkEl.href.startsWith('http') ? linkEl.href : 'https://www.airbnb.com' + linkEl.getAttribute('href')) : ''; const title = titleEl ? titleEl.textContent.trim() : ''; if (title) { experiences.push({ title: title, price: priceEl ? priceEl.textContent.trim() : '', rating: ratingEl ? ratingEl.textContent.trim() : '', review_count: reviewCountEl ? reviewCountEl.textContent.trim() : '', category: categoryEl ? categoryEl.textContent.trim() : '', duration: durationEl ? durationEl.textContent.trim() : '', image: imgEl ? (imgEl.getAttribute('src') || imgEl.getAttribute('data-original-uri') || '') : '', url: url }); } } return { location: `{{location}}`, result_count: experiences.length, experiences: experiences }",
      "name": "experiences_data",
      "label": "Extract experiences"
    }
  ]
}