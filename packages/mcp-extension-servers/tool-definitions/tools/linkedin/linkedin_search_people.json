{
  "id": "linkedin-search-people",
  "name": "linkedin_search_people",
  "description": "Search for people on LinkedIn by name, title, company, or keywords. Returns up to 10 results with name, headline, location, and profile URL.",
  "version": "1.0.0",
  "parameters": [
    {
      "name": "query",
      "type": "string",
      "description": "Search query (name, title, company, or keywords)",
      "required": true
    }
  ],
  "steps": [
    {
      "action": "navigate",
      "url": "https://www.linkedin.com/search/results/people/",
      "urlParams": {
        "keywords": "{{query}}"
      },
      "label": "Search LinkedIn people"
    },
    {
      "action": "waitForSelector",
      "selector": ".reusable-search__result-container, .search-results-container",
      "timeout": 15000,
      "label": "Wait for search results"
    },
    {
      "action": "wait",
      "ms": 2000,
      "label": "Let results render"
    },
    {
      "action": "scroll",
      "direction": "down",
      "amount": 800,
      "label": "Scroll to load more"
    },
    {
      "action": "wait",
      "ms": 1500
    },
    {
      "action": "code",
      "code": "const results = []; const cards = document.querySelectorAll('.reusable-search__result-container, li.reusable-search__result-container'); for (let i = 0; i < Math.min(cards.length, 10); i++) { const card = cards[i]; const nameEl = card.querySelector('.entity-result__title-text a span[aria-hidden=\"true\"], .entity-result__title-text a'); const headlineEl = card.querySelector('.entity-result__primary-subtitle'); const locationEl = card.querySelector('.entity-result__secondary-subtitle'); const linkEl = card.querySelector('.entity-result__title-text a, a.app-aware-link'); results.push({ name: nameEl ? nameEl.textContent.trim() : 'Unknown', headline: headlineEl ? headlineEl.textContent.trim() : '', location: locationEl ? locationEl.textContent.trim() : '', profile_url: linkEl ? linkEl.href.split('?')[0] : '' }); } return { result_count: results.length, results: results }",
      "name": "search_results",
      "label": "Extract search results"
    }
  ]
}
